# 🚀 Автодеплой Law Bot на Oracle Cloud через GitHub Actions

## 📋 Обзор

Теперь ваш бот может **автоматически обновляться** при каждом push в GitHub:

```
Вы делаете push в GitHub → GitHub Actions → SSH на сервер → Автообновление
```

---

## 📁 Созданные файлы

| Файл | Назначение |
|------|------------|
| `Dockerfile` | Образ для Telegram бота |
| `Dockerfile.admin` | Образ для админ-панели |
| `docker-compose.yml` | Оркестрация всех сервисов |
| `.env.docker` | Шаблон переменных окружения |
| `deploy.sh` | Скрипт первичного деплоя |
| `update.sh` | Скрипт ручного обновления |
| `.github/workflows/deploy.yml` | GitHub Actions для автодеплоя |
| `.gitignore` | Игнорируемые файлы |

---

## 🔧 Шаг 1: Подготовка GitHub репозитория

### 1.1: Создайте репозиторий на GitHub

```bash
# В директории проекта
cd C:\Users\HONOR\Documents\law_bot

# Инициализация git (если ещё не инициализирован)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit with Docker setup"
```

### 1.2: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `law-bot`)
3. **Не** инициализируйте его (оставьте пустым)

### 1.3: Привяжите локальный репозиторий к GitHub

```bash
# Добавьте удалённый репозиторий
git remote add origin https://github.com/ВАШ_USERNAME/law-bot.git

# Переименуйте ветку в main
git branch -M main

# Отправьте код
git push -u origin main
```

---

## 🔐 Шаг 2: Настройка Oracle Cloud сервера

### 2.1: Подключитесь к серверу по SSH

```bash
# Windows PowerShell
ssh -i C:\Users\HONOR\.ssh\id_rsa ubuntu@<PUBLIC_IP>
```

### 2.2: Запустите скрипт первичного деплоя

```bash
# На сервере
cd ~/law_bot

# Если ещё не клонировали репозиторий
bash deploy.sh
```

Скрипт:
- ✅ Установит Docker и Docker Compose
- ✅ Склонирует ваш репозиторий
- ✅ Создаст `.env` файл
- ✅ Соберёт и запустит контейнеры

### 2.3: Настройте `.env` файл

```bash
# На сервере
nano .env
```

**Заполните значения:**

```env
# Telegram Bot
BOT_TOKEN=8429912645:AAG95x5WDgqF8r42zFwnF8oLTPSGdQmMcUM
ADMIN_CHAT_ID=ваш_chat_id

# PostgreSQL
POSTGRES_DB=law_bot_db
POSTGRES_USER=law_bot_user
POSTGRES_PASSWORD=придумайте_сложный_пароль

# Admin Panel
ADMIN_PORT=8000

# Остальные настройки
DEBUG=False
MAX_FILE_SIZE=20971520
ALLOWED_EXTENSIONS=pdf,jpg,jpeg,png,doc,docx
```

### 2.4: Перезапустите контейнеры

```bash
docker-compose down
docker-compose up -d
```

---

## 🔑 Шаг 3: Настройка GitHub Secrets

Для автодеплоя GitHub Needs доступ к вашему серверу.

### 3.1: Создайте SSH ключ для GitHub Actions

```bash
# Локально (Windows PowerShell)
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions
# Нажмите Enter (пароль не нужен)
```

### 3.2: Скопируйте публичный ключ на сервер

```bash
# Windows PowerShell
scp -i C:\Users\HONOR\.ssh\id_rsa C:\Users\HONOR\.ssh\github_actions.pub ubuntu@<PUBLIC_IP>:~/github_actions.pub

# На сервере
ssh -i C:\Users\HONOR\.ssh\id_rsa ubuntu@<PUBLIC_IP>

# Добавьте ключ в authorized_keys
cat ~/github_actions.pub >> ~/.ssh/authorized_keys
rm ~/github_actions.pub
```

### 3.3: Добавьте секреты в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите: **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

Добавьте **4 секрета**:

| Название | Значение |
|----------|----------|
| `SERVER_HOST` | Публичный IP вашего сервера (например, `129.146.123.45`) |
| `SERVER_USERNAME` | Имя пользователя (обычно `ubuntu`) |
| `SERVER_PORT` | Порт SSH (обычно `22`) |
| `SSH_PRIVATE_KEY` | Содержимое файла `~/.ssh/github_actions` (приватный ключ) |

**Как получить приватный ключ:**

```bash
# Windows PowerShell
Get-Content C:\Users\HONOR\.ssh\github_actions | Set-Clipboard
```

Вставьте содержимое в поле секрета.

---

## ✅ Шаг 4: Проверка автодеплоя

### 4.1: Сделайте тестовое изменение

```bash
# Локально
# Внесите любое изменение в код

git add .
git commit -m "Тест автодеплоя"
git push origin main
```

### 4.2: Проверьте GitHub Actions

1. Откройте репозиторий на GitHub
2. Перейдите на вкладку **Actions**
3. Вы увидите запущенный workflow **Deploy to Oracle Cloud**
4. Дождитесь завершения (зелёная галочка ✅)

### 4.3: Проверьте сервер

```bash
# На сервере
docker-compose ps
docker-compose logs --tail=20
```

---

## 📊 Мониторинг и управление

### Полезные команды

```bash
# Статус сервисов
docker-compose ps

# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f bot
docker-compose logs -f admin
docker-compose logs -f postgres

# Перезапуск
docker-compose restart

# Пересборка
docker-compose build --no-cache

# Остановка
docker-compose down

# Обновление вручную
bash update.sh
```

---

## 🔧 Решение проблем

### Ошибка: "Permission denied (publickey)"

```bash
# Проверьте права на сервере
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### Ошибка: "docker-compose: command not found"

```bash
# Установите docker-compose
sudo apt update
sudo apt install -y docker-compose
```

### Ошибка: "Cannot connect to the Docker daemon"

```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Переподключитесь по SSH
exit
# Затем подключитесь снова
```

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs bot

# Проверьте .env
docker-compose exec bot env | grep BOT_TOKEN
```

### Админка недоступна

```bash
# Проверьте, что порт открыт
sudo netstat -tlnp | grep 8000

# Проверьте брандмауэр Oracle
# Console → VCN → Security List → Add Ingress Rule для порта 8000
```

---

## 🎯 Быстрый старт (чек-лист)

- [ ] 1. Создан GitHub репозиторий
- [ ] 2. Код отправлен в GitHub (`git push`)
- [ ] 3. Создан сервер Oracle Cloud
- [ ] 4. Запущен `deploy.sh` на сервере
- [ ] 5. Настроен `.env` файл
- [ ] 6. Создан SSH ключ для GitHub Actions
- [ ] 7. Добавлены 4 секрета в GitHub
- [ ] 8. Сделан тестовый push
- [ ] 9. GitHub Actions успешно отработал
- [ ] 10. Бот и админка работают

---

## 📞 Если что-то пошло не так

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте GitHub Actions: вкладка **Actions** в репозитории
3. Проверьте секреты: **Settings** → **Secrets and variables** → **Actions**

---

## 🎉 Готово!

Теперь при каждом `git push` ваш бот будет **автоматически обновляться** на сервере!

```bash
# Ваш рабочий процесс:
git add .
git commit -m "Новая фича"
git push origin main
# → Через 1-2 минуты обновление на сервере
```
