#!/bin/bash
# Скрипт для обновления кода на сервере
# Запускать при каждом обновлении в GitHub

set -e

echo "🔄 Law Bot - Обновление кода"

GREEN='\033[0;32m'
YELLOW='\033[1;33m'
NC='\033[0m'

PROJECT_DIR=~/law_bot
cd $PROJECT_DIR

# Pull новых изменений из Git
echo -e "${YELLOW}📥 Получение обновлений из репозитория...${NC}"
git pull origin main || git pull origin master

# Пересборка контейнеров
echo -e "${YELLOW}🔨 Пересборка контейнеров...${NC}"
docker-compose build

# Перезапуск сервисов
echo -e "${GREEN}🚀 Перезапуск сервисов...${NC}"
docker-compose up -d --force-recreate

# Ожидание запуска
sleep 5

# Вывод статуса
echo ""
echo -e "${GREEN}✅ Обновление завершено!${NC}"
echo ""
docker-compose ps
