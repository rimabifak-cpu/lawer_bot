# 🚀 Деплой Law Bot на Render

## 📋 Обзор

Render — это PaaS-платформа с **автоматическим деплоем из GitHub**.

**Преимущества:**
- ✅ Автодеплой при каждом `git push`
- ✅ Бесплатный тариф (Free)
- ✅ Встроенный PostgreSQL
- ✅ Не нужно управлять сервером
- ✅ HTTPS из коробки

**Ограничения Free тарифа:**
- ⚠️ Бот (worker) спит после 15 мин неактивности
- ⚠️ БД удаляется через 90 дней (нужно продлевать)
- ⚠️ Ограниченные ресурсы (512 MB RAM на сервис)

---

## 📁 Созданные файлы

| Файл | Назначение |
|------|------------|
| `render.yaml` | Конфигурация сервисов для Render |
| `Dockerfile.render` | Docker-образ для Render |
| `docker-compose.render.yml` | Для локального тестирования |

---

## 🔧 Шаг 1: Подготовка GitHub репозитория

### 1.1: Инициализация Git (если ещё не сделано)

```bash
cd C:\Users\HONOR\Documents\law_bot

git init
git add .
git commit -m "Initial commit for Render deploy"
```

### 1.2: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Создайте репозиторий (например, `law-bot`)
3. Оставьте пустым (без README)

### 1.3: Отправьте код в GitHub

```bash
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/law-bot.git
git push -u origin main
```

---

## 🔧 Шаг 2: Подключение к Render

### 2.1: Создайте аккаунт на Render

1. Перейдите на https://render.com
2. Нажмите **Get Started for Free**
3. Войдите через **GitHub** (рекомендуется) или email

### 2.2: Создайте новый проект

1. В дашборде нажмите **New +** → **Blueprint**
2. Подключите ваш GitHub репозиторий (`law-bot`)
3. Нажмите **Connect**

### 2.3: Настройте Blueprint

Render автоматически прочитает `render.yaml` и предложит создать:
- ✅ **law-bot-worker** (Background Worker) — для бота
- ✅ **law-bot-admin** (Web Service) — для админки
- ✅ **law-bot-db** (PostgreSQL) — база данных

Нажмите **Apply** для создания всех сервисов.

---

## 🔐 Шаг 3: Настройка переменных окружения

### 3.1: Добавьте переменные для Worker (бот)

1. В дашборде Render перейдите: **law-bot-worker** → **Environment**
2. Добавьте переменные:

| Key | Value |
|-----|-------|
| `BOT_TOKEN` | `8429912645:AAG95x5WDgqF8r42zFwnF8oLTPSGdQmMcUM` |
| `ADMIN_CHAT_ID` | Ваш chat_id (получить через @userinfobot) |

3. Нажмите **Save Changes**

### 3.2: Добавьте переменные для Admin Panel

1. Перейдите: **law-bot-admin** → **Environment**
2. Добавьте те же переменные:

| Key | Value |
|-----|-------|
| `BOT_TOKEN` | `8429912645:AAG95x5WDgqF8r42zFwnF8oLTPSGdQmMcUM` |
| `ADMIN_CHAT_ID` | Ваш chat_id |

3. Нажмите **Save Changes**

---

## ✅ Шаг 4: Проверка деплоя

### 4.1: Проверьте логи

1. **Worker**: law-bot-worker → **Logs**
2. **Admin**: law-bot-admin → **Logs**

В логах должно быть:
```
Starting bot...
INFO:     Application startup complete.
```

### 4.2: Проверьте бота

Откройте Telegram и отправьте `/start` вашему боту.

### 4.3: Проверьте админку

1. Перейдите: **law-bot-admin** → скопируйте URL (например, `https://law-bot-admin.onrender.com`)
2. Откройте в браузере: `https://law-bot-admin.onrender.com/api/test-db`

Должно вернуться:
```json
{"status": "success", "users_count": 0}
```

---

## 🔄 Шаг 5: Автообновление

Теперь при каждом `git push` Render будет **автоматически обновлять** сервисы:

```bash
# Внесите изменения локально
git add .
git commit -m "Новая фича"
git push origin main
```

Render автоматически:
1. Заберёт новый код из GitHub
2. Пересоберёт сервисы
3. Перезапустит контейнеры

**Статус деплоя:** вкладка **Events** в каждом сервисе.

---

## 📊 Мониторинг

### Логи

- **Worker**: https://dashboard.render.com/.../logs
- **Admin**: https://dashboard.render.com/.../logs

### Метрики

- **Usage**: потребление CPU/RAM
- **Requests**: количество запросов к админке

---

## ⚠️ Важные замечания

### 1. Бот может "засыпать"

На Free тарифе worker останавливается после 15 мин неактивности.

**Решения:**
- Перейти на платный тариф ($7/мес)
- Использовать сервис uptime-мониторинга (не рекомендуется)
- Развернуть на другом сервисе (Oracle Cloud, Railway)

### 2. PostgreSQL удаляется через 90 дней

Render удаляет бесплатные БД через 90 дней.

**Решение:**
- Продлить БД в дашборде перед истечением срока
- Или сделать дамп и восстановить новую

### 3. Ограничения Free тарифа

| Ресурс | Лимит |
|--------|-------|
| RAM | 512 MB |
| CPU | 0.5 vCPU |
| Bandwidth | 100 GB/мес |
| Build minutes | 750 часов/мес |

---

## 🔧 Решение проблем

### Ошибка: "Bot token is invalid"

Проверьте `BOT_TOKEN` в Environment variables.

### Ошибка: "DATABASE_URL is not set"

Render должен автоматически добавить `DATABASE_URL` из подключенной БД. Проверьте:
1. **law-bot-db** создана
2. В `render.yaml` правильно указано `fromDatabase`

### Ошибка: "ModuleNotFoundError"

Проверьте `requirements.txt` — все ли зависимости указаны.

### Админка возвращает 502

Подождите 1-2 минуты после деплоя. Сервис запускается ~30-60 сек.

---

## 🎯 Быстрый старт (чек-лист)

- [ ] 1. Код в GitHub репозитории
- [ ] 2. Аккаунт на Render создан
- [ ] 3. Blueprint применён (все 3 сервиса созданы)
- [ ] 4. Добавлены `BOT_TOKEN` и `ADMIN_CHAT_ID`
- [ ] 5. Бот отвечает на `/start`
- [ ] 6. Админка открывается
- [ ] 7. Тестовый push обновил сервисы

---

## 📞 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Pricing](https://render.com/pricing)
- [Blueprints](https://render.com/docs/blueprint-spec)

---

## 🎉 Готово!

Ваш бот и админка работают на Render!

```bash
# Рабочий процесс:
git add .
git commit -m "Изменения"
git push origin main
# → Через 2-3 минуты обновится на Render
```
